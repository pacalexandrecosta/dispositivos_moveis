<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="desenvolvimento.nassau.a006">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">





        <activity android:name=".view.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.LinearActivity"></activity>
        <activity android:name=".view.RelativeActivity"></activity>
        <activity android:name=".view.ListActivity001"></activity>
        <activity android:name=".view.ListActivity002"></activity>

    </application>

</manifest>